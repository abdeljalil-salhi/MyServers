version: '3.8'

services:
  mysql:
    image: mysql
    container_name: database
    env_file:
      - .env
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
